Read @TASK.md, pull the ticket from the jira MCP server, and fix the bug so the ticket's Definition of Done is met.
The ticket and the code are the source of truth.
Change only the one feature the ticket names. Verify the changes. Report when done.
