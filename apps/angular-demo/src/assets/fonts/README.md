# Fonts

Drop the **Rothorn** brand font here as `Rothorn.woff2` (from the Drive `04_Brand-family`
folder). `src/theme/plata.css` references `/assets/fonts/Rothorn.woff2`; until the file is
present the font stack falls through to Manrope / system sans with no build break.
