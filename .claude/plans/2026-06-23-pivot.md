# Pivot — runnable product + Claude Code + ccusage

**Date:** 2026-06-23. Supersedes the custom-agent-framework plan
(`2026-06-22-build-plan.md`), which built a self-contained TS agent loop + metrics.

## Why
The workshop should be a **basically working repository with a real product** that
launches, plus a **real coding agent configured via `.md`** that the participant
runs locally with their **own Anthropic key**, and a **market-standard local token
tracker** so they can measure → optimize → re-measure → compare.

Verified constraints that shaped this:
- **Cursor** cannot override the Anthropic base URL (only OpenAI), so its Anthropic
  traffic can't be metered through a local proxy. → agent = **Claude Code**.
- **LiteLLM** does not record spend for the Anthropic `/v1/messages` path Claude Code
  uses (issue #24204). → tracker = **ccusage** (reads Claude Code's local logs;
  no proxy, offline; separates cache tokens).

## Target architecture
- **Product:** runnable Angular design-system app (`npm start` → `ng serve`), with a
  mock `GET /api/components?q=` (HTTP interceptor + latency) so the catalog-search
  bug is visible in the browser. Ships buggy.
- **Agent:** Claude Code, configured by `AGENTS.md` (canonical) + `CLAUDE.md` +
  `.claude/settings.json`. Participant brings `ANTHROPIC_API_KEY`, talks directly to
  Anthropic (no proxy).
- **Tracker:** `ccusage` (`npx ccusage session`). Thin `tokens:measure`/`tokens:compare`
  wrappers snapshot cumulative ccusage totals and diff before/after.
- **Quality gate:** `scenario:verify` (kept) — mandatory hidden tests + typecheck +
  lint decide correctness. A token cut that breaks the gate doesn't count.
- **Task:** `TASK.md` — the Component Catalog Search fix (debounce, min-length,
  whitespace, stale-response cancellation, full state set), acceptance criteria,
  how to verify.

## Kept vs removed
- **Kept:** `apps/angular-demo` (made runnable), the scenario baseline + hidden
  reference suite (quality gate), `scenario:reset`/`scenario:verify` (decoupled to
  standalone), the doctor (reworked).
- **Removed:** `packages/{agent-core,agent-tools,context-builder,provider-adapters,token-metrics}`,
  `workshop/profiles`, the agent/token/context CLIs, scripted solver, traces/dashboard.

## Build steps
R1 record pivot · R2 delete framework + decouple tooling · R3 make the product run ·
R4 agent `.md` config · R5 ccusage measure/compare · R6 instructions + README ·
R7 verify (app serves; gate red→green; ccusage measures; doctor passes; reset to buggy).
